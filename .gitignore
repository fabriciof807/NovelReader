*.iml
.gradle
/local.properties
/.idea
/captures
.externalNativeBuild
.cxx
.kotlin
/build
/app/build
/release
/app/release
.DS_Store
Thumbs.db
*.apk
*.aab
*.keystore
local.properties
# Agent tooling state (installed locally per machine, never repo content)
.opencode
.agents/
/.worktrees/
/scripts/

# Landing page build output
landing-page/node_modules/
landing-page/dist/

# Security audit tool
piolium/

# Session handoffs (AI agent handoff documents — gitignored, never staged)
handoff-*.md

# Secrets and credentials (defense in depth — never commit these)
/secrets/
/credentials/
.env
.env.*
*.pem
*.key
*.jks
*.p12
*.pfx
key.properties
**/key.properties
google-services.json
firebase-credentials.json
.npmrc
.pypirc
.id_rsa
.id_dsa
.id_ecdsa
.id_ed25519
service-account*.json
*-credentials.json
*-service-account.json
